Backend-Retriever:
    - Use Vibed and remove the vibe-ish libraries from retriever/source/lib
    - Used vibed logger to log to the linux log
    - Test vibed MongoDB lib
    - Recover retriever settings and UserFilters from MongoDB
    - unittest the UserFilter
    - Insert the email into MongoDB (get the id)
    - Configure the indexer
    - Make the retriever index a copy of the message
    - Get a ConverstationID (new or existing from email references and/or reply-to)

Backend-API:

Frontend:
